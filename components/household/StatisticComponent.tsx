import { View } from "react-native";
import { Text } from "react-native-paper";

export default function StatisticComponent() {
  return (
    <View>
      <Text>Statistic Component</Text>
    </View>
  );
}
